#include <stdlib.h>

#include <string.h>

#include <math.h>

#include <stdio.h>

int main() {

    long long int i,t,b,f;
    scanf("%lld",&t);
    float a;
    while(t--)
        {
        
       scanf("%f",&a);
        scanf("%lld",&f);
        b=((a+1-f)*2);
            
            printf("%lld\n",b);
    
        
    }
        
    return 0;
}
