#include <stdlib.h>

#include <string.h>

#include <math.h>

#include <stdio.h>

int main()
{
int t;
scanf("%d",&t);
  long long int p,i,temp;
scanf("%lld",&p);
temp=p;
t=t-1;
while(t--)
{
 long long int n;
 scanf("%lld",&n);

 long long int a[n];
for(i=0;i<n-1;i++)
   scanf("%lld",&a[i]);
}
printf("%lld\n",temp);
return 0;
}